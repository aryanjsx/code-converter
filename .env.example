# CodexConvert Configuration
#
# CodexConvert does not require environment variables.
#
# AI providers are configured directly in the UI using the Provider Picker.
#
# API keys are stored locally in the browser session and are never
# transmitted to CodexConvert servers.
